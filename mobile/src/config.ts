// ================================================================
// URL de l'API backend.
// Par défaut : backend de production (Render).
// Pour tester en dev local, commente la ligne PROD et décommente
// la ligne DEV en remplaçant l'IP par celle de ta machine
// (Windows : ipconfig → "Adresse IPv4" de ta carte WiFi).
// Ne jamais utiliser "localhost" — Expo tourne sur le téléphone, pas le PC.
// ================================================================
export const API_URL = 'https://fittrack-qct1.onrender.com/api'
// export const API_URL = 'http://192.168.1.15:5000/api' // ← dev local
